#!/bin/sh
set -eu

script_dir=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
. "$script_dir/common.sh"

case ${1:-} in
    -h | --help)
        show_usage "scripts/stow" "Create symlinks for the selected dotfiles packages."
        exit 0
        ;;
esac

run_stow_action "stow" "" "$@"
